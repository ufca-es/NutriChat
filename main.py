from src.gui.iniciarChatGrafico import Root

if __name__ == "__main__":

    # isso é o que inicia a interface gráfica
    # que está localizada em src/gui/iniciarChatGrafico.py
    app = Root()
    app.mainloop()
